<li>
    {{burger_name}}

    <button class=" btn btn-secondary" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}} Delete
        {{else}} Devour
        {{/if}}
    </button>

</li>